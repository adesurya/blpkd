# requirements-gpu.txt
# Untuk: Dockerfile.gpu_worker SAJA
# Berisi: semua dari requirements-api.txt + insightface + ultralytics + onnxruntime-gpu
# CATATAN: torch dan torchvision TIDAK di sini — di-install terpisah di Dockerfile
#          via --index-url https://download.pytorch.org/whl/cu121

# ── Semua package dari requirements-api.txt ────────────────
-r requirements-api.txt

# ── Object Detection ───────────────────────────────────────
ultralytics==8.3.55

# ── Face Recognition (butuh g++ untuk compile dari source) ─
insightface==0.7.3

# ── ONNX Runtime GPU ───────────────────────────────────────
onnxruntime-gpu==1.20.1